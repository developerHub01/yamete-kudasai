# Assets

Please place your `yamete.mp3` file in this folder.

The extension is configured to look for `assets/yamete.mp3`. You can download any "Yamete Kudasai" sound clip and rename it to `yamete.mp3`.

If you prefer a different format (like `.wav`), you can update the path in `src/extension.ts`.
